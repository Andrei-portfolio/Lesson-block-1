package IntermediateCertification.Task4Reserve;

/* public class getComputerChoice {
    private static String getComputerChoice(Random random) {// Метод для генерации выбора компьютера
        int choice = random.nextInt(3);// Генерация случайного числа от 0 до 2
        switch (choice) {
            case 0: return "К"; // Камень
            case 1: return "Н"; // Ножницы
            case 2: return "Б"; // Бумага
            default: return ""; // Не должно произойти
        }
    }
}

 */
